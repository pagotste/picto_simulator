{
  "name": "pictosim",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "parcel index.html --open",
    "build": "cp -r ./static ./dist && parcel build index.html",
    "run": "npx parcel index.html"
  },
  "devDependencies": {
    "parcel": "^2.12.0",
    "parcel-plugin-static-files-copy": "^2.6.0",
    "parcel-reporter-static-files-copy": "^1.5.3",
    "serve": "^11.2.0"
  },
  "keywords": [],
  "dependencies": {
    "@parcel/config-default": "^2.12.0",
    "jquery": "^3.7.1"
  }
}
